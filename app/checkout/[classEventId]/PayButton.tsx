// PayButton is no longer used — payment logic is inlined in the checkout page.
// This file is kept to avoid breaking any existing references.

export function PayButton({ classEventId: _id }: { classEventId: string }) {
  return null;
}
